# 项目规则 — approval_feishu

## 子代理使用规则

1. **禁止使用子代理执行代码修改**。除非是制定计划（创建 `.omo/plans/*.md`），否则所有代码变更、调试、文件读写等操作必须由主 AI 直接完成，不得委托给任何子代理（task 工具）。

2. 允许使用子代理的唯一场景：**制定实施方案**（预生成 `.omo/plans/` 下的计划文件）。

3. 测试、构建、git 推送等操作同样由主 AI 直接执行。
